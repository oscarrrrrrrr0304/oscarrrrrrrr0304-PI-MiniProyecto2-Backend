# Configuración del servidor
PORT=3000
NODE_ENV=development

# Base de datos MongoDB
MONGODB_URI=mongodb://localhost:27017/miniproyecto2

# JWT Secret (cambia esto por un string seguro)
JWT_SECRET=tu_jwt_secret_super_seguro_aqui

# Para producción en Render, asegúrate de configurar estas variables de entorno:
# MONGODB_URI=tu_connection_string_de_mongodb_atlas
# JWT_SECRET=tu_jwt_secret_seguro
# NODE_ENV=production